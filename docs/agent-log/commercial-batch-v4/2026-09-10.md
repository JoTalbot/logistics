# Commercial Automation Batch V4 — 2026-09-10

## Scope
Advance market intelligence from provider snapshots into durable economics, explainable opportunity recommendations and operational human-review controls.

## Implemented

- Added `market_observation_history` as append-only historical provider snapshots.
- Hardened market-observation upsert so stale observations cannot overwrite newer current state.
- Added freshness checks and explicit verified-field extraction.
- Added deterministic route economics: direct cost, risk reserve, clean profit, risk-adjusted profit and margin rate.
- Added historical market-price trend bucketing and conservative recommended-price floor.
- Added durable `review_audit` records and explicit approve/reject/hold review service.
- Added authenticated review queue and metrics API.
- Added deterministic `recommend_opportunity()` combining route economics with compatible recent market evidence. Market data remains advisory and cannot lower the economic safety floor; currency mismatches are ignored rather than guessed.
- Added migration `0011_opportunity_recommendations.sql` to persist recommendation price, market median, reasons and update timestamp on canonical opportunities.
- Added `recommendation_store.py` for tenant-scoped persistence of recommendation outputs.
- Added authenticated `/api/v1/review/recommendations` exposure of persisted recommendation fields.
- Added authenticated `/api/v1/review/audit/report` with decision totals, daily decision trend and pending queue age.
- Added API reporting unit coverage.
- Added Docker Compose mount for migration `0011_opportunity_recommendations.sql`, fixing fresh local database initialization coverage.

## Verification

- Hosted CI Run #101 `34520924164` passed on the previous persisted-recommendation/status head.
- This reporting batch is now pushed to `main` and has triggered a fresh hosted CI run.
- Lardi smoke Run `34519177888` reached Lardi infrastructure but returned HTTP 403 Cloudflare Error 1010 / `browser_signature_banned`; this is classified as a provider-edge block requiring provider-side action.
- No retry loop, browser automation or anti-bot bypass was added.

## Current architecture

`Market provider → verified observations → immutable history → price trends → route economics → explainable recommendation → persisted Opportunity → operator dashboard/review`

`Telegram → canonical Load → normalize → score → Opportunity → pricing/matching → bounded negotiation → publication intent → policy gate → outbox`

`Operator → authenticated recommendations/audit report → explicit review decision → durable audit`

## Provider status

Lardi discovery remains read-only. Canonical provider-field mappings remain blocked until an actual authorized response is obtained. No field semantics are inferred from guessed JSON names.

## Next

1. Verify the reporting batch through hosted CI, including migration and API tests.
2. Re-run Lardi smoke only after provider support confirms the edge block is resolved or supplies an authorized API path.
3. Add provider-specific canonical mappings from verified response samples.
4. Activate external publication only after provider permission, commercial terms, privacy/retention and legal compliance are explicitly verified.
5. Expand to the next permitted market source using the same adapter/provenance/compliance gate.
6. Continue toward customer discovery, qualification and recurring-load automation without bypassing provider controls.

## Safety

No autonomous publication, financial commitment or provider-side mutation was added. Human review is explicit, authenticated and auditable.
