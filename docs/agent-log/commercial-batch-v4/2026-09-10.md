# Commercial Automation Batch V4 — 2026-09-10

## Scope
Advance market intelligence from a current observation snapshot to durable historical evidence, deterministic route economics and an explicit human-review boundary.

## Implemented

- Added `market_observation_history` as append-only historical provider snapshots.
- Hardened market-observation upsert so stale observations cannot overwrite newer current state.
- Added freshness checks and explicit verified-field extraction. Canonical route/country/cargo/weight/price fields must be allowlisted by an integration contract.
- Added deterministic route economics: direct cost, risk reserve, clean profit, risk-adjusted profit and margin rate.
- Added historical market-price trend bucketing and conservative recommended-price calculation.
- Added durable `review_audit` records.
- Added explicit human review service for publication intents and negotiation sessions.
- Added authenticated review API using `REVIEW_OPERATOR_TOKEN`; review approval never performs an external provider call.
- Added review queue metrics endpoint for pending publication/negotiation work and total review decisions.
- Fixed Docker Compose fresh-database initialization to include the complete migration chain, including duplicated historical migration sequence numbers through deterministic destination aliases.
- Added unit and PostgreSQL integration coverage for freshness, historical idempotency, verified-field extraction, route economics, trends and review validation.
- Hardened the Lardi smoke script to classify provider-edge blocks separately from credentials, permission, rate-limit and generic request failures.

## Verification

- Hosted CI Run #88 `34519826715` passed after the complete V4 batch: package installation, PostgreSQL migrations including `0010_market_intelligence.sql`, unit tests and integration tests.
- Manual Lardi smoke Run `34519177888` reached the provider but received HTTP 403 Cloudflare Error 1010 / `browser_signature_banned`. The credential was present and masked in Actions logs. This is recorded as a provider-edge block requiring provider-side action, not as a credential failure.
- No retry loop or bypass mechanism was added.

## Provider status

Lardi discovery remains read-only. The manual smoke workflow remains the only live provider operation. Live canonical field mapping remains blocked until a verified provider response is obtained.

## Safety

No autonomous publication, financial commitment or provider-side mutation was added. Human review is explicit, authenticated and auditable.

## Next

1. Re-run the manual Lardi smoke only after Lardi/provider support confirms the edge block is resolved or an authorized API access path is provided.
2. After verified provider responses, add provider-specific canonical route/country/cargo/weight/price mappings without guessing field semantics.
3. Connect historical price trends and route economics to opportunity scoring and explainable recommendations.
4. Add dashboard presentation and audit reporting around the review metrics endpoint.
5. Activate a provider publication adapter only after current permission/compliance evidence exists.
6. Expand to the next permitted market source only after the same adapter/provenance/compliance gate.
