# Research checkpoint — 2026-09-17

## Task
Continue verification and hardening of `JoTalbot/logistics` after V43.10.

## Repository state checked
- `STATUS.md`: V43.10, software/test contour green; production activation externally blocked.
- `AGENTS.md`: mandatory research gate, status/log updates, immediate commit/push discipline, and fail-closed safety boundaries.
- `README.md`: AI Logistics OS architecture and V1→V10 autonomy model.
- No `ROADMAP.md` or `CHANGELOG.md` exists at repository root.
- No open pull requests were found.
- Latest commits add credential reenrollment fencing, race-safe idempotency, Bearer authentication hardening, and status evidence.

## Relevant implementation
- `backend/logistics/remote_control.py` enforces explicit Bearer credentials, credential-generation fencing, lease checks, and conservative AUTO/REVIEW/BLOCK classification.
- `deploy/remote-agent/agent.py` uses POSIX process groups and kills the process group on timeout/lease loss.
- `deploy/remote-agent/cgroup_rehearsal.py` provides an opt-in systemd transient-scope rehearsal with detached-descendant verification and `cgroup.kill` evidence.
- Runtime per-task cgroup enforcement is intentionally not enabled yet.

## External research
- Linux kernel cgroup v2 documentation: https://docs.kernel.org/admin-guide/cgroup-v2.html
  - cgroup v2 uses hierarchical controllers, has a no-internal-process constraint, and delegated subtrees are containment boundaries.
  - `cgroup.kill` provides subtree kill semantics; post-spawn PID migration is not equivalent to pre-membership containment.
- systemd transient scopes remain the repository's selected candidate boundary for target-host rehearsal.

## Decision
Do not enable runtime per-task cgroup enforcement from CI evidence alone. The repository's existing gate requiring an authorized non-root target-host rehearsal remains correct. The next implementation gate is successful target-host evidence showing the task and a detached descendant share the transient scope and both terminate via `cgroup.kill`.

## Current blockers
- No authorized target host is available through this GitHub-only execution surface.
- Vercel status remains externally blocked.
- Lardi/provider access and publication/contact authorization remain external gates.

## Next step
Run the existing opt-in target-host cgroup rehearsal under the dedicated non-root logistics-agent identity, capture evidence, and only then consider enabling runtime cgroup enforcement.
